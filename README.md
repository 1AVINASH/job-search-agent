# programming-facts-daily
This program will provide details of companies that are hiring for remote job along with some required details

* Package manager is uv
* To run this through docker, simply run docker compose up --build -d (Remove the build flag if not running for the first time. Add -d for running in detached state)